{
  "name": "scribemed-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "Placeholder monorepo scaffolding for Scribemed CI/CD pipeline.",
  "packageManager": "pnpm@8.15.4",
  "scripts": {
    "lint": "pnpm -r --stream run lint",
    "format:check": "node scripts/ci/pipeline-task.js format:check",
    "type-check": "node scripts/ci/pipeline-task.js type-check",
    "migrate": "node scripts/ci/pipeline-task.js migrate",
    "test:unit": "pnpm -r --stream run test",
    "test:integration": "pnpm -r --stream run test",
    "coverage": "node scripts/ci/pipeline-task.js coverage",
    "build": "pnpm -r --stream run build"
  }
}
