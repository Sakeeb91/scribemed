FROM node:20-alpine

# Placeholder Dockerfile for the coding service.
# Replace with the actual runtime image and build steps when the service is implemented.
WORKDIR /app
COPY package.json .
CMD ["node", "-e", "console.log('Coding service container placeholder')"]
