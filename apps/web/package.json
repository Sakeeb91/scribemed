{
  "name": "@scribemed/web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "echo 'Web app dev server - to be implemented'",
    "lint": "pnpm exec eslint . --ext .ts,.tsx,.js,.jsx --ignore-pattern dist --ignore-pattern build",
    "type-check": "pnpm exec tsc --noEmit",
    "test": "echo 'Web app tests - to be implemented'",
    "build": "echo 'Web app build - to be implemented'",
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\""
  }
}
