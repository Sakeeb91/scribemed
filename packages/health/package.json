{
  "name": "@scribemed/health",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "pretest": "pnpm run build",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "dependencies": {
    "@scribemed/database": "workspace:*",
    "@scribemed/logging": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "rimraf": "^6.0.1",
    "typescript": "^5.2.0"
  }
}
